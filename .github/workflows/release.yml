name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write  # needed to create GitHub Releases

jobs:
  # ── macOS (universal binary: arm64 + amd64) ──────────────────────────────────
  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Build (darwin/universal)
        working-directory: desktop
        run: wails build -platform darwin/universal -clean

      - name: Package .app as zip
        working-directory: desktop
        run: |
          ditto -c -k --keepParent \
            build/bin/dictate-assistant.app \
            dictate-assistant-macos-universal.zip

      - uses: actions/upload-artifact@v4
        with:
          name: macos
          path: desktop/dictate-assistant-macos-universal.zip

  # ── Windows (amd64, NSIS installer) ──────────────────────────────────────────
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Build (windows/amd64)
        working-directory: desktop
        run: wails build -platform windows/amd64 -nsis -clean

      - uses: actions/upload-artifact@v4
        with:
          name: windows
          path: desktop/build/bin/dictate-assistant-amd64-installer.exe

  # ── Linux (amd64) ────────────────────────────────────────────────────────────
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          # ubuntu-latest (24.04 "Noble") dropped libwebkit2gtk-4.0-dev entirely —
          # only 4.1 dev headers are packaged now. Requires -tags webkit2_41 below.
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Build (linux/amd64)
        working-directory: desktop
        run: wails build -platform linux/amd64 -tags webkit2_41 -clean

      - name: Package as tar.gz
        working-directory: desktop
        run: |
          tar czf dictate-assistant-linux-amd64.tar.gz \
            -C build/bin dictate-assistant

      - uses: actions/upload-artifact@v4
        with:
          name: linux
          path: desktop/dictate-assistant-linux-amd64.tar.gz

  # ── GitHub Release ────────────────────────────────────────────────────────────
  release:
    needs: [build-macos, build-windows, build-linux]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: artifacts/*
